namespace IOChef.Core
{
    /// <summary>
    /// Represents the current state of the game.
    /// </summary>
    public enum GameState
    {
        MainMenu,
        LevelSelect,
        Playing,
        Paused,
        GameOver,
        Results
    }
}
